docker-compose up
docker-compose exec l4acados_dev bash



////////incase there are problems with gpytorch and l4acados
-> for gpytorch
pip install gpytorch

-> for l4acados
cd /workspace
-> check fr l4acados in the src folder and install it
ls -la src/l4acados
cd /workspace
python3 setup.py develop
pip3 install -e src/

-> add to the python path
export PYTHONPATH=/workspace/src:$PYTHONPATH

-> run to test for install

python3 -c "import l4acados; print('l4acados found')"
